# You can use this file to create a conda environment for this pipeline:
#   conda env create -f environment.yml
name: teaching-assembly-2021
channels:
  - bioconda
  - conda-forge
  - defaults
dependencies:
  - gaas
  - prokka=1.14.6
  - megahit=1.2.9
